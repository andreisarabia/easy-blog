"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const is_str = (val) => val && typeof val === 'string';
exports.is_str = is_str;
const is_fn = (val) => val && typeof val === 'function';
exports.is_fn = is_fn;
const is_arr = (val) => val && Array.isArray(val);
exports.is_arr = is_arr;
const is_str_arr = (val) => is_arr(val) && val.every(is_str);
exports.is_str_arr = is_str_arr;
const is_fn_arr = (val) => is_arr(val) && val.every(is_fn);
exports.is_fn_arr = is_fn_arr;
const is_truthy = (val) => Boolean(val);
exports.is_truthy = is_truthy;
const filter_out_falsey = (val) => val.filter(is_truthy);
exports.filter_out_falsey = filter_out_falsey;
const generate_random_int = (min, max) => {
    min = Math.ceil(min);
    return Math.floor(Math.random() * (Math.floor(max) - min)) + min;
};
exports.generate_random_int = generate_random_int;
